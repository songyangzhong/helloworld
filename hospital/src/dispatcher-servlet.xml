<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:p="http://www.springframework.org/schema/p"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="
       http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.0.xsd
       http://www.springframework.org/schema/mvc http://www.springframework.org/schema/mvc/spring-mvc-4.0.xsd
       http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-4.0.xsd
">
    
    <mvc:annotation-driven />
    
    <!--spring mvc容器应该只管理控制器组件-->
    <!--默认component-scan开启对所有组件的扫描,@Controller, @Service, @Repository, @Compoentn-->
    <!--如果将use-default-filers属性设置为false,则不自动对四种组件进行扫描,需要自行指定扫描哪些组件-->
    <!--type指定根据什么区分要扫描的组件,这里根据注解-->
    <!--expression用于指定注解的全限定名或类名的正则表达式表示-->
    <context:component-scan base-package="com.niit">
        <context:include-filter type="annotation"
            expression="org.springframework.stereotype.Controller"/>
    </context:component-scan>
        
    <bean class="org.springframework.web.servlet.view.InternalResourceViewResolver"
            id="internalResourceViewResolver">
        <!-- 前缀 -->
        <property name="prefix" value="/WEB-INF/page/" />
        <!-- 后缀 -->
        <property name="suffix" value=".jsp" />
    </bean>

	 <!--mvc名称空间下的resources标记用于为静态资源指定处理方式-->
    <!--也就是用于配置需要放过对哪些静态资源的访问-->
    <!--location用于指定静态资源的位置-->
    <!--mapping:指定访问静态资源的路径,带css或js或images的请求放过-->
    
    <mvc:resources mapping="/resources/**" location="/resources/" />
    
    <!--<mvc:resources mapping="/css/**" location="/css/" />-->
    <!--<mvc:resources mapping="/js/**" location="/js/" />-->
    <!--<mvc:resources mapping="/images/**" location="/images/" />-->
    
    <bean id="multipartResolver" class="org.springframework.web.multipart.commons.CommonsMultipartResolver">  
          <property name="defaultEncoding" value="utf-8" />  
          <property name="maxUploadSize" value="1024000" />  
          <property name="maxInMemorySize" value="40960" />  
    </bean> 

</beans>
